# torch.permute_copy

torch.permute_copy()

Performs the same operation as [`torch.permute()`](torch.permute.html#torch.permute), but all output tensors
are freshly created instead of aliasing the input.
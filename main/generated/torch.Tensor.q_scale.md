# torch.Tensor.q_scale

Tensor.q_scale() → [float](https://docs.python.org/3/builtins/functions.html#float)

Given a Tensor quantized by linear(affine) quantization,
returns the scale of the underlying quantizer().
# torch.squeeze_copy

torch.squeeze_copy()

Performs the same operation as [`torch.squeeze()`](torch.squeeze.html#torch.squeeze), but all output tensors
are freshly created instead of aliasing the input.
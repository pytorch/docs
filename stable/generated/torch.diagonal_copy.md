# torch.diagonal_copy

torch.diagonal_copy()

Performs the same operation as [`torch.diagonal()`](torch.diagonal.html#torch.diagonal), but all output tensors
are freshly created instead of aliasing the input.